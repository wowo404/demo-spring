# 启用并发执行
junit.jupiter.execution.parallel.enabled=true
# 设置并行模式：同一线程内或跨线程
junit.jupiter.execution.parallel.mode.default=concurrent
# 可选：设置并发线程数（默认为 CPU 核心数）
junit.jupiter.execution.parallel.config.dynamic.factor=8
junit.jupiter.execution.parallel.config.fixed.percentage=100
# 或者直接指定线程数：
# junit.jupiter.execution.parallel.config.strategy=fixed
# junit.jupiter.execution.parallel.config.fixed.parallelism=8
# junit.jupiter.execution.parallel.config.fixed.max-pool-size=16